"""
Настройки бота.

BOT_TOKEN берётся из переменной окружения BOT_TOKEN (так безопаснее —
токен не хранится в коде и не попадёт в Git-репозиторий). Если
переменная не задана — используется запасное значение ниже (удобно
для локального запуска на своём компьютере).
"""
import os

# Токен вашего бота, полученный у @BotFather.
# На хостинге (Bothost и т.п.) задайте BOT_TOKEN в настройках
# переменных окружения проекта — тогда эта строка не используется.
BOT_TOKEN = os.getenv("BOT_TOKEN", "ВСТАВЬТЕ_СЮДА_ТОКЕН_ДЛЯ_ЛОКАЛЬНОГО_ЗАПУСКА")

# Username админов бота, БЕЗ символа "@", регистр не важен.
# У этих людей появляется доступ к скрытым командам /export, /import,
# /edit и /delete.
ADMIN_USERNAMES = {"vvmp8", "roman_graf_platinum", "Mr_Leg1on"}
